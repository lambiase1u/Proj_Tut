ngDescribe({
    name: 'Test eventForm component',
    modules: 'app',
    element: '<eventForm></eventForm>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
